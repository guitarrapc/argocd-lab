# no namespace declared here for argocd to manage it
apiVersion: v1
kind: Service
metadata:
  name: api-csharp-svc
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: api-csharp
---
apiVersion: v1
kind: Service
metadata:
  name: api-go-svc
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: api-go
